{
  "name": "vanillaJS",
  "version": "1.0.0",
  "description": "100 days of code",
  "main": "avi.js",
  "repository": "git@github.com:avimehenwal/vanillaJS.git",
  "author": "avimehenwal <avi.mehanwal@gmail.com>",
  "license": "MIT",
  "scripts": {
    "js": "tsc --watch",
    "pre:build": "./scripts/prepare-docs.sh",
    "css": "stylus --watch ./frontend/styles.styl --out docs",
    "clean": "rm -rfv docs",
    "dev": "yarn clean && yarn pre:build && ./scripts/start-development.sh $(pwd)"
  },
  "devDependencies": {
    "stylus": "^0.54.8",
    "typescript": "^4.2.4"
  }
}